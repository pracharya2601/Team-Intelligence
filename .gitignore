# Secrets
.env
.env.local

# Lane lock (per-machine; each dev sets via scripts/setup-lane.sh)
.lane

# Planning / project docs (not part of the skills work)
BUILD-PLAN.md
SPONSORS.md
TECH-STACK.md
requirements/
research/
